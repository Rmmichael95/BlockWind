# =========================
# WordPress / Theme basics
# =========================
# Only relevant if this repo sometimes sits inside a WP install checkout
wp-config.php
wp-content/uploads/
wp-content/cache/
wp-content/upgrade/
wp-content/backups-dup-pro/
wp-content/ai1wm-backups/

# =========================
# OS junk (macOS / Windows / Linux)
# =========================
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
.Trash-*
.fseventsd

Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk

# Linux file manager metadata
.directory

# =========================
# Editor junk (VS Code / Vim / Neovim)
# =========================
# Vim/Neovim swap/backup/undo files
*.swp
*.swo
*.swn
*.swm
*.un~
*~
*.tmp
*.bak
*.orig

# Vim/Neovim session files
Session.vim
Sessionx.vim

# Optional: local vim/nvim config files people sometimes generate
# (comment out if you intentionally commit these)
# .vim/
# .nvim/

# VS Code: ignore the folder but allow sharing team settings
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/launch.json
!.vscode/tasks.json

# JetBrains
.idea/
*.iml

# =========================
# Node / Vite / Tailwind
# =========================
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
pnpm-store/
.pnpm-store/

# Tool caches
.vite/
.vite-cache/
.eslintcache
.stylelintcache

# Tailwind caches (rare, but safe)
.tailwind/

# Sass caches (if any tooling writes here)
.sass-cache/

# =========================
# Build output
# =========================
# Theme build output (your Vite/Tailwind dist)
assets/dist/

# If you instead COMMIT compiled assets (common for WP themes),
# comment the line above and ignore only sourcemaps:
# assets/dist/**/*.map

# =========================
# Env files / secrets
# =========================
.env
.env.*
!.env.example

# =========================
# PHP tooling (optional)
# =========================
vendor/
composer.lock

# =========================
# Logs / Testing / Coverage
# =========================
*.log
coverage/
.nyc_output/

# =========================
# IMPORTANT: Git submodules
# =========================
# /assets/blocks/* are git submodules.
# Do NOT ignore /assets/blocks/** here.
# Each block repo should manage its own ignores (node_modules/, build/, etc.) within the submodule.

