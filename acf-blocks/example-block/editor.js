/**
 * BlockWind ACF Block: Example — editor.js (editor-only)
 *
 * Compiles to: editor.min.js
 * Use only if you need editor behavior beyond CSS.
 */

// Keep editor-only behavior lightweight.
// If you need WP packages, bundle them or enqueue via PHP with dependencies.
