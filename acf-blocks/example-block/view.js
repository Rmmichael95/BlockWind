/**
 * BlockWind ACF Block: Example — view.js (frontend)
 *
 * Compiles to: view.min.js
 */

document.addEventListener("DOMContentLoaded", () => {
  document.querySelectorAll(".bw-example").forEach((el) => {
    // behavior goes here
  });
});
